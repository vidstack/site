---
description: This hook is used to subscribe to specific slider state updates.
---

This hook can be used within any slider instance like so:

::code[./usage]

A ref is required if called outside outside of a slider instance:

::code[./with-ref]
