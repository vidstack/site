---
title: Svelte Installation Guide
description: Instructions to get your player installed and on-screen using Svelte.
---

import MediaIconsSetup from '~/docs/installation/partials/media-icons-setup.mdx';

import IfNoLayout from '~/docs/installation/conditions/if-no-layout.svelte';
import IfSSR from '~/docs/installation/conditions/if-ssr.svelte';
import AddPlayerMarkup from '~/docs/installation/partials/add-player-markup.mdx';
import AddPoster from '~/docs/installation/partials/add-poster.mdx';
import BrowserSupport from '~/docs/installation/partials/browser-support.mdx';
import ImportLibrary from '~/docs/installation/partials/import-library.mdx';
import InstallPackages from '~/docs/installation/partials/install-packages.mdx';
import KeepAlive from '~/docs/installation/partials/keep-alive.mdx';
import NextSteps from '~/docs/installation/partials/next-steps.mdx';
import SetPlaysinline from '~/docs/installation/partials/set-playsinline.mdx';
import SetupBundler from '~/docs/installation/partials/setup-bundler.mdx';
import VSCodeSetup from '~/docs/installation/partials/vscode-setup.mdx';
import BundlerSelection from '~/docs/installation/selection/bundler-selection.astro';
import FrameworkSelection from '~/docs/installation/selection/framework-selection.astro';
import ProviderSelection from '~/docs/installation/selection/provider-selection.astro';
import StylingSelection from '~/docs/installation/selection/styling-selection.astro';

## 1. Select Framework

<FrameworkSelection />

## 2. Select Bundler

<BundlerSelection />

## 3. Select Provider

<ProviderSelection />

## 4. Select Styling

<StylingSelection />

<IfNoLayout client:load>

:::warning
Be careful of the [svelte-preprocess][svelte-pre] auto-processor when building your own layout as
it will strip away `<template>` tags that are used in certain player components. Either specify
the preprocessors you need or change the `markupTagName` to something other than "template".
:::

</IfNoLayout>

## 5. Check Support

<BrowserSupport />

## 6. Install Packages

<InstallPackages />

## 7. Add Types

If you're using TypeScript, add the following to your `tsconfig` to enable fully-typed
custom elements:

::code[./tsconfig]{title="tsconfig.json" copy=true}

## 8. Setup Bundler

<SetupBundler />

[svelte-pre]: https://github.com/sveltejs/svelte-preprocess

## 9. Import Library

<ImportLibrary />

## 10. Player Markup

<AddPlayerMarkup />

<IfSSR client:load>

:::warning
There's a long-standing [hydration bug][svelte-hydrate-bug] with Svelte 4 and Custom Elements which
has been resolved in Svelte 5. If you're using Svelte 4, you will need to dynamically import the
player or [disable SSR][svelte-no-ssr]:

::code[./ssr]{copy=true}

:::

</IfSSR>

[svelte-no-ssr]: https://kit.svelte.dev/docs/page-options#ssr
[svelte-hydrate-bug]: https://github.com/sveltejs/svelte/issues/7379

## 11. Poster (Optional)

<AddPoster />

## 12. Playsinline (Optional)

<SetPlaysinline />

## 13. Visual Studio (Optional)

<VSCodeSetup />

## 14. Media Icons (Optional)

<MediaIconsSetup />

## 15. Keep Alive (Optional)

<KeepAlive />

## 16. Examples

You should be all set up by this point. We recommend exploring our 👉 [examples][examples] to
learn how to start building and customizing your player. You can also use the examples to make
sure you've set everything up correctly.

[examples]: https://github.com/vidstack/examples/tree/main/player/svelte

## 17. Next Steps

<NextSteps />
